import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exp-ionic-zoom-video-sdk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
